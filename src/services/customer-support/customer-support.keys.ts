import { general } from '../general.keys';
const _mainUri = general._mainUri;


export const _customerSupport = {
    _saleOrder: _mainUri + '/saleOrder',
    _returnProduct: _mainUri + '/returnProduct'
}

