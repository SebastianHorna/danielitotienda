import { _productManagement } from '../product-management.keys';
const _price = _productManagement._price;

export const _productUris = {
  _price: _price + '/price',
  _kindPrice: _price + '/kindPrice',
};
