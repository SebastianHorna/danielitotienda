import { _productManagement } from '../product-management.keys';
const _product = _productManagement._product;

export const _productUris = {
  _brand: _product + '/brand',
  _category: _product + '/category',
  _product: _product + '/product',
  _size: _product + '/size',
};
